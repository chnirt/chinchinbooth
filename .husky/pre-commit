#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Pre-commit: Updating .env.local with APP_VERSION..."
sh scripts/update-version.sh

# Optionally, add .env.local to staging if you want to track changes:
# git add .env.local
